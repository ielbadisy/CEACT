% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ceac.R
\name{plot_ceac}
\alias{plot_ceac}
\title{Plot Cost-Effectiveness Acceptability Curve (CEAC)}
\usage{
plot_ceac(x, data = NULL, wtp_range = seq(0, 1e+05, 1000), ...)
}
\arguments{
\item{x}{Either a result object from \code{\link[=boot_icer]{boot_icer()}}, or a formula of the form \code{cost + effect ~ group}
or \code{cost + effect ~ 1} (in this case, \code{group} is ignored and the comparison is made internally).}

\item{data}{A data frame, required if \code{x} is a formula.}

\item{wtp_range}{A numeric vector of WTP thresholds (default: \code{seq(0, 100000, 1000)}).}

\item{...}{Additional arguments passed to \code{ggplot2::labs()} (e.g. \code{subtitle}).}
}
\value{
A \code{ggplot} object displaying the CEAC.
}
\description{
This function plots the CEAC, which displays the probability that an intervention is cost-effective
compared to a reference, across a range of willingness-to-pay (WTP) thresholds. It can take as input
either a \code{boot_icer} object or a formula interface using cost and effect variables.
}
\details{
When using a formula, the cost and effect variables must appear on the left-hand side (e.g., \code{cost + effect ~ group}).
If \code{x} is a \code{boot_icer} object, the CEAC is based on the bootstrapped distribution of incremental cost and effect.
}
\examples{
# Example with boot_icer object
set.seed(123)
df <- data.frame(
  c = c(rnorm(100, 500, 100), rnorm(100, 200, 100)),
  e = c(rnorm(100, 0.6, 0.05), rnorm(100, 0.65, 0.06)),
  g = rep(c("control", "treatment"), each = 100)
)

res <- boot_icer(c + e ~ g, data = df, ref = "control", R = 500)
plot_ceac(res, wtp_range = seq(0, 20000, 500))

# Example with only cost and effect
plot_ceac(c + e ~ 1, data = df, wtp_range = seq(0, 20000, 500))

}
